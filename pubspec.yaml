name: amddasfoods
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.2.0+3

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  cupertino_icons: 
  google_fonts: 
  http: 
  flutter_secure_storage: 
  jwt_decoder: 
  shared_preferences: 
  carousel_slider:
  flutter_datetime_picker_plus: 
  table_calendar: ^3.0.0
  flutter_datetime_picker: ^1.5.1 # New DateTime Picker Package
  provider:
  syncfusion_flutter_datepicker:

  # Add other dependencies below
  flutter_native_splash: 
  quickalert:   # Add this line for QuickAlert
  url_launcher: 
  intl:  # Add this line for date formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 
  flutter_launcher_icons: 

# Configuration for flutter_native_splash
flutter_native_splash:
  color: "#ffffff" # Background color for splash screen
  image: images/amd.png # Path to your splash image
  android: true
  ios: true

# Configuration for flutter_launcher_icons
flutter_icons:
  android: true
  ios: true
  image_path: "images/amdicon.png"  # Path to your icon file

flutter:
  uses-material-design: true

  assets:
    - images/amd.png
    - gif/login.gif
    - gif/login2.gif
    - gif/login3.gif
    - gif/forgot.gif
    - gif/confirmotp.gif
    - gif/reset.gif
    - gif/signup.gif
    - gif/failed.gif
    - gif/sucess.gif
    - gif/otp.gif
    - gif/user.gif
    - gif/confirmotp.gif
    - images/food.jpg
    - images/borg.png